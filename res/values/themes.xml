<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: 2024 The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <!-- Settings activity theme. -->
    <style name="Theme.AudioFx" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:listPreferredItemPaddingLeft">24dp</item>
        <item name="android:listPreferredItemPaddingStart">24dp</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="alertDialogTheme">@style/Theme.AudioFx.AlertDialog</item>
        <item name="materialAlertDialogTheme">@style/Theme.AudioFx.AlertDialog</item>
    </style>

    <!-- Settings alert dialog theme. -->
    <style name="Theme.AudioFx.AlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="android:colorBackground">?attr/colorSurface</item>
        <item name="dialogCornerRadius">16dp</item>
    </style>

    <!-- Toolbar style -->
    <style name="Theme.AudioFx.ToolbarCollapsedTextAppearance" parent="TextAppearance.Material3.ActionBar.Title" />

    <style name="Theme.AudioFx.ToolbarExpandedTextAppearance" parent="TextAppearance.Material3.HeadlineMedium">
        <item name="android:drawablePadding">10dp</item>
        <item name="android:textSize">36sp</item>
    </style>

</resources>
